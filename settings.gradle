pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        maven { url 'https://maven.scijava.org/content/repositories/public/' } // Add this line
        jcenter()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven { url 'https://maven.scijava.org/content/repositories/public/' } // Add this line
        jcenter()
    }
}
rootProject.name = "FaceRecognition"
include ':app'
include ':libfacesdk'
